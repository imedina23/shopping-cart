@model shopping.Data.Entities.Category

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
     <partial name="_CategoryDetails"  />

</div>

<div>
     <a asp-action="AddState" class="btn btn-outline-primary" asp-route-id="@Model?.Id">Add State</a> 
    <a asp-action="Edit" class="btn btn-outline-warning" asp-route-id="@Model?.Id">Edit Country</a> 
    <a asp-action="Index" class="btn btn-outline-success">Back to List</a>
</div>
