@model shopping.Data.Entities.City

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
     <partial name="_CityDetails"  />

</div>

<div>
  
    <a asp-action="EditCity" class="btn btn-outline-warning" asp-route-id="@Model?.Id">Edit</a> 
    <a asp-action="DetailsState" class="btn btn-outline-success"asp-route-id="@Model?.State.Id">Back to List</a>
</div>
